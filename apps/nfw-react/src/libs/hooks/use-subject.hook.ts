import { Observable } from "rxjs";
import { UniversalComponent } from "../component/universal-component.class";

export function useSubject(target: UniversalComponent, subject: Observable<any>) {
  const subscription =

  target.subscriptions.push(

  )

  return
}
